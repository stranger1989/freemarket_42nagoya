= render "shared/header"

.main-container
  .mypage-container
    .main-column.main-column--user-info
      .main-column__title
        %h2.main-column__title__title 完了
      .main-column__confirm
        確認済みです

    = render 'shared/side-bar'

.app-banner

= render 'shared/footer'

= render 'shared/sell_btn'
